# Benzersiz Son Kelimeli Doğru Şıklar (Tek Cevaplı)

Sadece tek doğru cevabı olan sorular. Doğru şıkkın son kelimesi, aynı sorudaki hiçbir yanlış şıkta son kelime değil.

Toplam: **152** doğru şık

| Soru | Şık | Doğru Cevap | Son Kelime |
|---|---|---|---|
| 2 | B | Cluster IP | **ip** |
| 7 | B | gcloud init | **init** |
| 8 | C | Ask each member of the team to generate a new SSH key pair and to add the public key to their Google account. Grant the compute.osAdminLogin role to the Google group corresponding to this team. | **team.** |
| 13 | B | 10.0.0.0/8 | **10.0.0.0/8** |
| 17 | A | Select Cloud SQL (MySQL). Verify that the enable binary logging option is selected. | **selected.** |
| 19 | D | In the Google Cloud Platform Console, create a new billing account and set up a payment method. | **method.** |
| 20 | B | GCP Cloud Identity and Access Management | **management** |
| 22 | D | Primitive roles can be assigned to Google accounts, service accounts, and Google App domains. | **domains.** |
| 23 | A | Use gcloud iam roles copy and specify the production project as the destination project. | **project.** |
| 29 | D | Use GCP Marketplace to launch the Jenkins solution. | **solution.** |
| 34 | A | Load balancing | **balancing** |
| 35 | B | Create an instance template, and use the template in a managed instance group with autoscaling configured. | **configured.** |
| 36 | D | second-by-second | **second-by-second** |
| 41 | A | When creating the VM via the web console, specify the service account under the ‘Identity and API Access' section. | **section.** |
| 42 | C | Admin Activity and System Event audit logs | **logs** |
| 43 | B | Install a RDP client in your desktop. Set a Windows username and password in the GCP Console. Use the credentials to log in to the instance. | **instance.** |
| 44 | C | Cloud Armor analyzes requests at a secure perimeter outside your VPC. | **vpc.** |
| 46 | A | VM-attachable persistent disk storage | **storage** |
| 47 | D | Execute the Deployment Manager template using the -preview option in the same project, and observe the state of interdependent resources. | **resources.** |
| 48 | B | Project ID | **id** |
| 49 | D | Cloud Pub/Sub, Cloud Dataflow, Cloud Bigtable, a SS ee BigQuery ann 4 EE Co pt | **pt** |
| 50 | C | roles/owner | **roles/owner** |
| 52 | D | All of the Above | **above** |
| 54 | D | Cloud CDN | **cdn** |
| 58 | C | Assigning Liens | **liens** |
| 61 | A | Create a signed URL with a four-hour expiration and share the URL with the company. | **company.** |
| 62 | C | You are not charged for querying data that is cached within BigQuery. | **bigquery.** |
| 64 | A | Use primitive roles by default | **default** |
| 65 | A | Enable the Cloud Pub/Sub API in the API Library on the GCP Console. | **console.** |
| 66 | A | 250 GB | **gb** |
| 67 | C | Configure a single Stackdriver account, and link all projects to the same account. | **account.** |
| 68 | A | Cloud IAM | **iam** |
| 70 | A | Application State | **state** |
| 71 | C | Navigate to the project and then to the IAM section in the GCP Console. Review the members and roles. | **roles.** |
| 73 | B | Verify that you are Project Billing Manager for the GCP project. Create a new billing account and link the new billing account to the existing project. | **project.** |
| 74 | A | Billing Account Creator | **creator** |
| 76 | C | Cloud Armor | **armor** |
| 82 | A | Storing and querying massive datasets | **datasets** |
| 89 | D | Increase the initial delay of the HTTP health check to 200 seconds. | **seconds.** |
| 90 | A | By using forwarding rules | **rules** |
| 92 | D | Register the parent domain only - subdomain records are not required. | **required.** |
| 93 | C | On the App Engine Versions page of the GCP Console, route 100% of the traffic to the previous version. | **version.** |
| 95 | D | Go to Cloud Shell and run gcloud config list to review the Google Cloud configuration used for deployment. | **deployment.** |
| 96 | D | Firestore | **firestore** |
| 98 | C | Cloud Storage Coldline | **coldline** |
| 107 | B | Perform a rolling-action start-update with maxSurge set to 1 and maxUnavailable set to 0. | **0.** |
| 108 | B | Cloud Spanner | **spanner** |
| 111 | B | Create an export to the sink that saves logs from Cloud Audit to a Coldline Storage bucket. | **bucket.** |
| 112 | B | Run it on Compute Engine, and choose a custom instance type with 6 vCPUs and 32 GB of memory. | **memory.** |
| 113 | D | 1. Using Cloud VPN or Interconnect, create a tunnel to a VPC in Google Cloud. 2. Use Cloud Router to create a custom route advertisement for 199.36.153.4/30. Announce that network to your on-premises network through the VPN tunnel. 3. In your on-premises network, configure your DNS server to resolve *.googleapis.com as a CNAME to restricted.googleapis.com. | **restricted.googleapis.com.** |
| 114 | C | 1. Create a service account. 2. Give the Cloud Run Invoker role to that service account for your Cloud Run application. 3. Create a Cloud Pub/Sub subscription that uses that service account and uses your Cloud Run application as the push endpoint. | **endpoint.** |
| 115 | A | Deploy the container on Cloud Run. | **run.** |
| 118 | C | 1. In Cloud Logging, create a filter to view only Compute Engine logs. 2. Click Create Export. 3. Choose BigQuery as Sink Service, and the platform-logs dataset as Sink Destination. | **destination.** |
| 120 | B | Use gcloud to create a key file for the service account that has appropriate permissions. | **permissions.** |
| 121 | A | In the project where the images are stored, grant the Storage Object Viewer IAM role to the service account used by the Kubernetes nodes. | **nodes.** |
| 125 | A | Run gcloud projects list to get the project ID, and then run gcloud services list --project <project ID>. | **id>.** |
| 126 | D | Deploy a new version of your application in App Engine. Then go to App Engine settings in GCP Console and split traffic between the current version and newly deployed versions accordingly. | **accordingly.** |
| 128 | A | Create a Kubernetes Service of type Node Port for your application, and a Kubernetes Ingress to expose this Service via a Cloud Load Balancer. | **balancer.** |
| 129 | B | Verify that both projects are in a GCP Organization. Share the VPC from one project and request that the Compute Engine instances in the other project use this shared VPC. | **vpc.** |
| 133 | B | Grant them the required IAM roles using their G Suite email address. | **address.** |
| 137 | C | Add a network tag of your choice to the instance. Create a firewall rule to allow ingress on UDP port 636 for that network tag. | **tag.** |
| 139 | A | When creating the VM, use machine type n1-standard-96. | **n1-standard-96.** |
| 141 | D | Set up Cloud VPN between the infrastructure on-premises and Google Cloud. | **cloud.** |
| 143 | A | Go to Data Catalog and search for employee_ssn in the search box. | **box.** |
| 145 | D | Go to the Stackdriver Logging console, review admin activity logs, and filter them for Cloud Spanner IAM roles. om | **om** |
| 150 | A | Modify the existing subnet range to 172.16.20.0/24. | **172.16.20.0/24.** |
| 153 | B | 1. Advise your developers to go to Cloud Marketplace. 2. Ask the developers to launch a Cassandra image for their development work. | **work.** |
| 154 | B | 1. Create a Cloud Monitoring Workspace and associate your Google Cloud Platform (GCP) project with it. 2. Create a Cloud Monitoring Alerting Policy that uses the threshold as a trigger condition. 3. Configure your email address in the notification channel. | **channel.** |
| 155 | D | Create a Cloud Monitoring alerting policy to send an alert to webhook when Cloud Spanner CPU is over <r under your threshold. Create a Cloud Function that listens to HTTP and resizes Spanner resources accordingly. a | **a** |
| 156 | C | Export the billing data to BigQuery. Create a Cloud Function that uses BigQuery to sum the egress network costs of the exported billing data for the Apache web server for the current month and sends an email if it is over 100 dollars. Schedule the Cloud Function using Cloud Scheduler to run hourly. | **hourly.** |
| 157 | C | Give €aproject owner€ arole to crm-databases-proj and bigquery.dataViewer role to web- applications. | **applications.** |
| 163 | B | Create a snapshot schedule for the disk using the desired interval. | **interval.** |
| 164 | B | Assign the auditor the IAM role roles/logging.privateLogViewer. Direct the auditor to also review the logs for changes to Cloud IAM policy. | **policy.** |
| 166 | A | 1. Verify that you are assigned the Project Owners IAM role for this project. 2. Locate the project in the GCP console, click Shut down and then enter the project ID. | **id.** |
| 167 | D | Add the user to roles/iam.serviceAccountAdmin role. a | **a** |
| 169 | A | Enable node auto-provisioning on the GKE cluster. | **cluster.** |
| 171 | A | Create a new project, enable the Compute Engine and Cloud SQL APIs in that project, and replicate the setup you have created in the development environment. | **environment.** |
| 172 | D | Enable deletion protection on the instance. | **instance.** |
| 173 | D | Install the cloud-datastore-emulator component using the gcloud components install command. a | **a** |
| 174 | C | Add the users to a group, and add this group to roles/browser. | **roles/browser.** |
| 175 | B | In Cloud Identity, set up SSO with a third-party identity provider with Google as a service provider. | **provider.** |
| 178 | D | Create a secondary index using the following Data Definition firstname, « lastname Language (DDL): Penance profile picture ) ee | **ee** |
| 180 | D | Add your SREs to a group and then add this group to roles/accessapproval.approver role. a | **a** |
| 181 | C | Create a budget per project and configure budget alerts on all of these budgets. | **budgets.** |
| 182 | D | Allow only a single Service Account access to read the data. | **data.** |
| 184 | B | Use Cloud Functions and configure the bucket as a trigger resource. | **resource.** |
| 185 | D | Expand the CIDR range of the relevant subnet for the cluster. | **cluster.** |
| 188 | D | Ask the operations partner to generate SSH key pairs, and add the public keys to the VM instances. | **instances.** |
| 189 | B | Create a new Node Pool and specify machine type n2highmem16. Deploy the new pods. | **pods.** |
| 190 | D | Create two separate BigQuery external tables on Cloud Storage and Cloud Bigtable. Use the BigQuery console to join these tables through user fields, and apply appropriate filters. | **filters.** |
| 191 | A | You want to adjust your design to support the failure of a single Compute Engine zone, eliminate downtime, and minimize cost. What should you do? | **do?** |
| 192 | D | Use the command gcloud projects getiampolicy to view the current role assignments. | **assignments.** |
| 194 | A | Create the instance without a public IP address. | **address.** |
| 196 | B | 1. In the Cloud Console, go to the Compute Engine Disks page and select your instance's disk. 2. In the Snapshot Schedule section, select Create Schedule and configure the following parameters: Schedule frequency: Daily Start time: 1:00 AM 2:00 AM Autodelete snapshots after 30 days | **days** |
| 197 | A | Deploy Jenkins through the Google Cloud Marketplace. | **marketplace.** |
| 198 | A | Set the europe-west1-d zone as the default zone using the gcloud config subcommand. | **subcommand.** |
| 199 | D | Ingest the data into Cloud Bigtable. Create a row key based on the event timestamp. SE a SS Ch) 0 zamsnyesnse == S wou | **wou** |
| 200 | D | Enable API, create a workspace under project A, and then add project B and C. @ _ sabitses e | **e** |
| 201 | B | Grant the VM Service Account the role Storage Object Creator on corp-aggregate-reports-storage. | **corp-aggregate-reports-storage.** |
| 202 | B | Create a service account with appropriate access for Google services, and configure the application to use this account. | **account.** |
| 204 | D | Create an Instance Template with Preemptible VMs On. Create a Managed Instance Group from the template and adjust Target CPU Utilization. Migrate the workload. | **workload.** |
| 206 | D | Install and configure the Cloud Logging Agent and view the logs from Cloud Logging. inswer(s): D ‘eference: ttps://cloud.google.com/error-reporting/docs/setup/compute-engine ‘loud Loging knows nothing about applications installed on the system without an agent collecting logs. Using the serial console is ot a best-practice and is impractical on a large scale. he VM images for Compute Engine and Amazon Elastic Compute Cloud (EC2) don't include the Logging agent, so you must omplete these steps to install it on those instances. The agent runs under both Linux and Windows. Source: [ips.72c loud.goOOgle.cOl logging do agent/logging/installatlo Ups 77ecl aaa iene OGGINGTAOCSTAGENTTONTING Sa com Qe» | **qe»** |
| 210 | A | Sync Identities with Cloud Directory Sync, and then enable SAML for single sign-on | **sign-on** |
| 212 | B | Create a private zone on Cloud DNS, and configure the applications with the DNS name. | **name.** |
| 224 | A | Meet with the cloud enablement team to discuss load balancer options. | **options.** |
| 226 | A | Deploy the web application on Google Kubernetes Engine standard edition with an internal ingress. | **ingress.** |
| 229 | D | Use the Google Cloud Pricing Calculator to determine the cost of every Google Cloud resource you expect to use. Use similar size instances for the web server, and use your current on-premises machines as a comparison for Cloud SQL. | **sql.** |
| 235 | A | Deploy the application on GKE Autopilot. | **autopilot.** |
| 237 | B | Change the subnet IP range from 10.0.0.0/20 to 10.0.0.0/18. | **10.0.0.0/18.** |
| 242 | D | Lift and shift to a VM on Compute Engine. Use an instance schedule to start and stop the instance. | **instance.** |
| 243 | D | + Create multiple service accounts, one for each pipeline with the appropriate minimal Identity and Access Management (IAM) permissions. + Use a secret manager service to store the key files of the service accounts. + Allow the CI/CD pipeline to request the appropriate secrets during the execution of the pipeline. | **pipeline.** |
| 244 | A | Turn on Data Access Logs for the buckets they want to audit, and then build a query in the log viewer that filters on Cloud Storage. | **storage.** |
| 245 | B | Create a script that uses the gcloud storage command to synchronize the on-premises storage with Cloud Storage, Schedule the script as a cron job. | **job.** |
| 246 | D | Create a custom role by removing delete permissions. Add users to the group, and then add the group to the custom role. | **role.** |
| 250 | D | Use the command gcloud projects get"€aiam"€apolicy to view the current role assignments. | **assignments.** |
| 254 | D | Use Storage Transfer Service to move the data from the selected on-premises file storage systems to a Cloud Storage bucket. | **bucket.** |
| 255 | A | Select the MIG from the Compute Engine console and, in the menu, select Replace VMs. | **vms.** |
| 257 | C | Review the error message that User A received. | **received.** |
| 258 | D | Migrate your data to AlloyDB. | **alloydb.** |
| 260 | D | kubectl get pods -I “app=prod" | **“app=prod"** |
| 263 | A | Deploy Jenkins through the Google Cloud Marketplace. | **marketplace.** |
| 264 | B | Create two separate BigQuery external tables on Cloud Storage and Cloud Bigtable. Use the BigQuery console to join these tables through user fields, and apply appropriate filters. | **filters.** |
| 267 | C | Create a budget per project and configure budget alerts on all of these budgets. | **budgets.** |
| 269 | D | Ingest the data into Bigtable. Create a row key based on the event timestamp. | **timestamp.** |
| 270 | B | Search for the CMS solution in Google Cloud Marketplace. Deploy the solution directly from Cloud Marketplace. | **marketplace.** |
| 271 | A | Enable the Cloud Spanner API. | **api.** |
| 272 | A | Develop templates for the environment using Cloud Deployment Manager. | **manager.** |
| 274 | A | Assign appropriate access for Google services to the service account used by the Compute Engine VM. | **vm.** |
| 276 | A | Create a new project and create an App Engine instance in europe-west3 | **europe-west3** |
| 277 | B | Create a new Node Pool and specify machine type n2"€ahighmem .16"€aDeploy the new pods. | **pods.** |
| 278 | C | Create a custom log-based metric for the specific error to be used in an Alerting Policy. | **policy.** |
| 279 | C | Multiregional Cloud Storage bucket. | **bucket.** |
| 280 | B | Grant the VM Service Account the role Storage Object Creator on corp-aggregate-reports- storage. | **storage.** |
| 281 | D | + Add all developers to a Google group in Cloud Identity. + Create a custom role with Compute Engine, Cloud Functions, and Cloud SQL permissions at the Google Cloud organization level. + Assign the custom role to the Google group. | **group.** |
| 282 | B | Use the gcloud compute ssh command with the --tunnel-through-iap flag. Allow ingress traffic from the IP range 35.235.240.0/20 on port 22. | **22.** |
| 285 | A | Set the minimum number of instances for your Cloud Run service to 3. | **3.** |
| 291 | D | Your managed instance group's VM's are toggled to only last 1 minute inpreemptible settings. | **settings.** |
| 294 | C | Configure a single Stackdriver account, and link all projects to the same account. | **account.** |
| 299 | B | Create the GKE cluster with Workload Identity Federation. Create a Google service account and a Kubernetes ServiceAccount, and configure both service accounts to use Workload Identity Federation. Attach the Kubernetes ServiceAccount to the application Pods and configure the Google service account to access the bucket with Identity and Access Managemeni (IAM). | **(iam).** |
| 301 | C | Ingest your application logs to Cloud Logging by using Ops Agent, and explore your logs with Log Analytics. | **analytics.** |
| 303 | B | Enable Cloud Storage object versioning on your bucket. Configure Pub/Sub notifications for your Cloud Storage buckets. | **buckets.** |
| 304 | B | Set up Cloud Monitoring alerts on the data freshness metric for the Dataflow jobs to receive a notification when a certain threshold is reached. | **reached.** |
| 308 | B | Organize the users in Cloud Identity into groups. Enforce multi-factor authentication in Cloud Identity. | **identity.** |
| 313 | C | Enable Private Google Access on the subnet within the custom VPC. | **vpc.** |
| 314 | D | Set the CLOUDSDK_PROXY_USERNAME and CLOUDSDK_PROXY_PASSWORD properties by using environment variables in your command line tool. | **tool.** |
| 320 | C | Use a custom mode VPC network, use Cloud Router border gateway protocol (BGP) routes, and use active/passive ro.'ting. | **ro.'ting.** |
| 323 | C | + Navigate to the Audit Logs page in the Google Cloud console, and enable Data Read, Data Write and Admin Read logs for the Bigtable instance. + Create a Pub/Sub topic as a Cloud Logging sink destination, and add your SIEM as a subscriber to the topic. | **topic.** |
| 327 | D | Deploy your solution to an instance group, and set the autoscaling based on CPU utilization. | **utilization.** |
| 329 | B | Use Cloud Spanner to store user data mapped to the game statistics. | **statistics.** |
| 330 | D | Create a Cloud Function triggered by Cloud Storage bucket events to submit the file URI to the Google Speech-to-Text API. | **api.** |
| 332 | B | Use Cloud Logging filters to create log-based metrics for firewall and instance actions. Monitor the changes and set up reasonable alerts. | **alerts.** |
| 335 | B | Use the gcloud services enable compute.googleapis.com command to enable Compute Engine and the gcloud services enable storage-api.googleapis.com command to enable the Cloud Storage APIs. | **apis.** |
