# Benzersiz Son 2 Kelimeli Doğru Şıklar

Benzersiz son kelime ve benzersiz ilk kelime listelerindeki sorular hariç. Doğru şıkkın **son iki kelimesi**, aynı sorudaki hiçbir yanlış şıkkın son iki kelimesiyle eşleşmiyor.

Toplam: **16** soru

| Soru | Şık | Doğru Cevap | Son 2 Kelime |
|---|---|---|---|
| 5 | D | Jennifer deployed an alpha cluster | **alpha cluster** |
| 39 | A | Use Cloud Storage Object Lifecycle Management using Age conditions with SetStorageClass and Delete actions. Set the SetStorageClass action to 90 days and the Delete action to 275 days | **275 days** |
| 59 | B | Storage Admin | **storage admin** |
| 60 | A | The Flexible Environment supports App Engine applications on configurable Compute Engine instances. | **engine instances.** |
| 87 | B | Store the database password inside a Secret object. Modify Era: the YAML file to populate the DB_LPASSWORD environment Tabels: variable from the Secret. Pa —— | **pa ——** |
| 91 | C | Select Compute Engine. Use preemptible VM instances of the appropriate standard machine type. | **machine type.** |
| 105 | A | 1. Go to the Logs ingestion window in Stackdriver Logging, and disable the log source for the GKE container resource. | **container resource.** |
| 117 | A | Add the support team group to the roles/monitoring.viewer role | **roles/monitoring.viewer role** |
| 147 | A | For each Google Cloud product in the solution, review the pricing details on the products pricing page. Use the pricing calculator to total the monthly costs for each Google Cloud product. | **cloud product.** |
| 179 | C | Add the group for the finance team to roles/billing viewer role. | **viewer role.** |
| 217 | C | Run gcloud config configurations list followed by gcloud config configurations activate. | **configurations activate.** |
| 225 | B | Configure Cloud Identity-Aware Proxy for SSH and TCP resources. | **tcp resources.** |
| 241 | C | Instruct the external consultant to generate an SSH key pair, and request the public key from the consultant. Add the public key to the instance yourself, and have the consultant access the instance through SSH with their private key. | **private key.** |
| 273 | C | Enforce an org policy constraint allowing the lifetime of service account keys to be 24 hours. Enforce an org policy constraint denying service account key creation with an exception on pj-sa. | **on pj-sa.** |
| 311 | B | Configure Cloud Identity-Aware Proxy for SSH and TCP resources | **tcp resources** |
| 324 | D | Enable Data Access audit logs for the Cloud Storage API. | **storage api.** |
